/** Largeur à partir de laquelle la disposition tablette (sidebar) s'active. */
export const TABLET_BREAKPOINT = 768;

/** Largeur maximale d'une colonne de contenu sur grand écran. */
export const CONTENT_MAX_WIDTH = 560;
