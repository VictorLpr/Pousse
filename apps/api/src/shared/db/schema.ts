/**
 * Schéma physique (ADR-0002, ADR-0005) : source de vérité unique, les
 * migrations sont générées depuis ce fichier par drizzle-kit
 * (`npm run db:generate -w apps/api`).
 *
 * Les onze relations du modèle logique restent à déclarer (voir
 * docs/modelisation/pousse_mcd_final_v4.html).
 */
export {};
