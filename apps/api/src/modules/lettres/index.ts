import type { FastifyInstance } from 'fastify';

/**
 * Module lettres : une lettre par mois et par enfant (ADR-0005). Pas encore
 * d'écran côté mobile — module réservé pour ce chantier futur.
 */
export async function lettresModule(_app: FastifyInstance): Promise<void> {
  // Routes à venir.
}
