import type { FastifyInstance } from 'fastify';

/**
 * Module defis : tirage hebdomadaire des défis, trophées (ADR-0005 : trois
 * défis proposés par enfant et par semaine, un seul engagé à la fois).
 *
 * À implémenter : service de tirage hebdomadaire, routes défis/trophées.
 */
export async function defisModule(_app: FastifyInstance): Promise<void> {
  // Routes à venir.
}
